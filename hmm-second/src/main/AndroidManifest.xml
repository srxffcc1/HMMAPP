<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.health.second">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name="com.healthy.library.base.BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/LibAppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".activity.TechnicianDetailActivity"
            android:exported="false" />
        <activity
            android:name=".activity.ShopDetailActivity"
            android:exported="true" />
        <activity android:name=".activity.SecondShopListActivity" />
        <activity android:name=".activity.SecondServiceDetailActivity" />
        <activity
            android:name=".activity.OrderTicketActivity"
            android:exported="true" />
    </application>

</manifest>